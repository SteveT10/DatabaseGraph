This folder is used to contain the database file generated from Python. 
The C++ program and Python Script detect each other through a relative 
directory path which is hardcoded. Features will not work if
the current folder structure is deleted.